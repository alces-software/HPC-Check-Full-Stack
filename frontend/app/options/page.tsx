import Options from '../components/Settings';

export const metadata = {
   title: 'Options'
};

export default function OptionsPage() {
   return (
      <main>
         <Options />
      </main>
   );
}
