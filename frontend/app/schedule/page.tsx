import Schedule from '../components/Schedule';

export const metadata = {
   title: 'Schedule'
};

export default function SchedulePage() {
   return (
      <main>
         <Schedule />
      </main>
   );
}
