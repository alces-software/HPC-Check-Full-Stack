import Instructions from "../components/clusterInstructions"

export const metadata = {
  title: "Results"
};

export default function ClusterInstructions() {
  return (
    <main>
      <Instructions />
    </main>
  );
}